# 홈화면 데이터 명세

독립만세팀 / KB AI Challenge 2026
프론트(홈화면)에서 실제로 화면에 뿌리는 값 전부와, 각각의 출처를 정리한 문서.
백엔드 스키마 / API 설계할 때 참고용.

---

## 0. 한눈에 보기

홈화면 값의 출처는 5가지로 나뉨.

| 구분 | 설명 | 개수 |
|---|---|---|
| ① 마이데이터 | 계좌·카드 연동으로 들어오는 원천 데이터 | 원천 |
| ② 사용자 입력 | 회원가입 + 자취 목표 설정에서 받음 | 6개 |
| ③ 계산 | ①②를 조합해 백엔드가 계산 | 12개 |
| ④ GPT 생성 | AI 분석 시점에 생성 후 **저장** | 4개 |
| ⑤ 외부 API | 정책 / KB 금융상품 | 홈화면엔 없음 |

**중요**: ④ GPT 생성 문구는 홈을 열 때마다 호출하면 안 됨.
`다시 분석하기` 누를 때만 생성하고 결과를 DB에 저장 → 홈은 저장된 값을 읽기만.
(비용 + 응답속도 + 매번 문구가 바뀌는 문제)

---

## 1. 화면 요소별 필요 데이터

### 1-1. 상단 헤더

| 화면 표시 | 필드 | 출처 |
|---|---|---|
| `안녕하세요, 도현님` | `user.name` | ② 회원가입 |
| 알림 뱃지 `3` | `notification.unreadCount` | ③ 계산 (미확인 알림 수) |
| 프로필 이미지 | `user.profileImageUrl` | ② (없으면 기본 이미지) |

### 1-2. 사이드바 — 최근 접속 정보

| 화면 표시 | 필드 | 출처 |
|---|---|---|
| 마이데이터 업데이트 `2025.07.20` | `mydata.lastSyncedAt` | ① 연동 시각 |
| AI 분석 리포트 확인 `2025.07.23` | `analysis.lastViewedAt` | ③ 조회 로그 |

### 1-3. 히어로 배너

| 화면 표시 | 필드 | 출처 | 계산식 |
|---|---|---|---|
| `D+14개월 후 자취 가능!` | `monthsToGoal` | ③ | `ceil(남은금액 ÷ 월저축액)` |
| `목표 보증금 30,000,000원` | `goal.targetDeposit` | ② 자취 목표 설정 |
| `다음 분석 예정일 2025.08.24` | `analysis.nextScheduledAt` | ③ | `마지막 분석일 + 30일` |
| `(30일 후)` | — | ③ | `nextScheduledAt - 오늘` |

### 1-4. 자금 현황 카드

| 화면 표시 | 필드 | 출처 | 계산식 |
|---|---|---|---|
| 도넛 `42%` | `progressRate` | ③ | `모은금액 ÷ 목표보증금 × 100` |
| 현재 모은 금액 `12,400,000원` | `savedAmount` | ③ | 계좌잔액 + 예금 + 적금 + 투자자산 |
| 앞으로 모아야 할 금액 `17,600,000원` | `remainingAmount` | ③ | `목표보증금 - 모은금액` |
| 대출 가능 금액 `20,000,000원` | `loanAvailableAmount` | ③ | DSR 한도 기반 추정 |
| 월 저축 목표 `500,000원` | `monthlySavingGoal` | ③ 또는 ② | AI 제안값 (사용자가 수정 가능하게 할지 결정 필요) |
| 예상 월 저축 가능액 `520,000원` | `expectedMonthlySaving` | ③ | `월평균소득 - 월평균소비` |

> `월 저축 목표(50만)` < `예상 저축 가능액(52만)` 이라 지금 시안은
> "여유 있음" 상태. 반대 케이스(목표가 더 큰 경우) UI도 필요한지 결정 필요.

### 1-5. 자취 가능 시점 예측 카드

| 화면 표시 | 필드 | 출처 |
|---|---|---|
| 목표 보증금 `30,000,000원` | `goal.targetDeposit` | ② |
| `이전 분석(2025.06.24) 대비 2개월 단축` | `previousPlan.{analyzedAt, monthsToGoal}` | ③ **DB 저장 필수** |
| 현재 저축 속도 `D+14개월` `(2026년 09월)` | `plan.current.{months, targetDate}` | ③ |
| 대출 활용 시 `D+8개월` `(2026년 03월)` | `plan.withLoan.{months, targetDate}` | ③ |
| 꺾은선 그래프 (점 5개) | `plan.*.trend[]` | ③ 월별 누적 저축 시뮬레이션 |
| `월 저축액 500,000원 기준` | `monthlySavingGoal` | ③ |
| 대출 활용 시뮬레이션 토글 | `goal.useLoan` | ② 자취 목표 설정의 "대출 의향" |

**꺾은선 그래프 데이터 형태** — 지금은 숫자 5개 배열이면 됨.

```json
"trend": [10, 28, 44, 62, 80]
```

월별 누적 저축액을 5구간으로 샘플링한 값. 실제 금액이든 0~100 정규화값이든 상관없음
(프론트에서 최대·최소 잡아 알아서 그림). 점 개수를 늘리려면 배열만 길게 주면 됨.

### 1-6. AI 추천 인사이트 카드

| 화면 표시 | 필드 | 출처 |
|---|---|---|
| `식비를 월 80,000원 절약하면 / 자취 가능 시점이 2개월 앞당겨져요!` | `insight.headline` | ④ GPT |
| 절약 팁 TOP 3 (라벨 + 금액) | `insight.tips[]` | ③ 소비 카테고리 상위 3개 → ④ GPT가 문구화 |

팁 항목 형태:

```json
{ "label": "배달/외식 줄이기", "amount": -40000, "category": "food" }
```

> 팁 3개는 **소비 데이터에서 뽑는 게 맞음**(순수 GPT 창작이면 근거가 없음).
> 백엔드가 카테고리별 지출을 또래 평균과 비교 → 초과분 상위 3개 추출 → GPT가 문장만 다듬는 구조 추천.

### 1-7. 오늘의 체크리스트

| 항목 | 상태 판정 기준 |
|---|---|
| 마이데이터 연결 확인 | 연동 계좌 1개 이상 → `완료` |
| 자취 시뮬레이션 실행 | 분석 이력 1건 이상 → `완료` |
| 자금조달 설계 확인 | 해당 페이지 조회 여부 → `진행중` / `대기` |
| 추천 금융상품 비교 | 상품 상세 조회 여부 |
| 정책·지원금 신청 확인 | 정책 상세 조회 여부 |

상태값 3종: `완료` / `진행중` / `대기`

> 전부 **사용자 행동 로그**로 판정됨. 별도 테이블 하나 필요
> (`user_id`, `step_key`, `status`, `updated_at`).
> MVP에서 부담되면 페이지 방문 여부만 boolean으로 저장해도 됨.

### 1-8. 하단 AI 자취 코치 배너

| 화면 표시 | 필드 | 출처 |
|---|---|---|
| 코치 한 마디 (2줄) | `coach.message` | ④ GPT |
| 저축 목표 달성 팁 `목표 달성 확률 87%!` | `coach.successRate` | ③ 계산 근거 필요 |
| DSR 안전 범위 `현재 DSR 21%` | `coach.dsrRate` | ③ `연간 원리금상환액 ÷ 연소득` |
| 추천 지역 `성북구 인근 / 월 8~15만원 절약` | `coach.region.{name, savingRange}` | ③ 공공데이터(한국부동산원 평균 월세) 비교 |

> **`목표 달성 확률 87%`는 근거를 정해야 함.** 지금은 시안용 숫자인데,
> 심사에서 "이 87%는 어떻게 나온 건가요?" 질문 나오기 딱 좋은 지점.
> 간단하게라도 규칙을 정하는 걸 추천 (예: 최근 3개월 저축액이 목표를 달성한 비율).

---

## 2. 마이데이터 원천 항목 (회의 결정 구조)

홈화면이 직접 쓰진 않지만, 위 계산값들의 재료.

```
소득 정보
└ 월 평균 소득

소비 정보
├ 식비 / 문화·취미 / 쇼핑 / 기타 소비
└ 고정비용
   └ 교통비 / 통신비 / 보험료 / 구독료 / 대출이자 / 주거비용

자산 정보
└ 계좌 잔액 / 예금 / 적금 / 투자 자산 / 대출 / 남은 상환금액
```

홈화면에서 실제로 쓰이는 건:

- `월 평균 소득`, `월 평균 소비` → 예상 월 저축 가능액
- `계좌잔액 + 예금 + 적금 + 투자자산` → 현재 모은 금액
- `대출 + 남은 상환금액 + 대출이자` → DSR, 대출 가능 금액
- `소비 카테고리별 금액` → 절약 팁 TOP 3

---

## 3. DB 저장이 반드시 필요한 것

### 3-1. 분석 이력 (재분석 비교용) — **회의에서 저장하기로 결정한 부분**

`이전 분석(2025.06.24) 대비 2개월 단축` 문구를 만들려면 과거 분석 결과가 남아 있어야 함.

한 번의 분석마다 저장할 값:

```
analysis_id, user_id, analyzed_at,
target_deposit, monthly_saving,
months_to_goal, target_date,
months_to_goal_with_loan, target_date_with_loan,
saved_amount, progress_rate,
insight_headline, coach_message   ← GPT 결과도 같이 저장
```

프론트는 **최신 1건 + 직전 1건**만 있으면 됨.
`previousPlan`이 없으면(최초 분석) 비교 문구는 자동으로 숨겨지게 만들어놨음.

### 3-2. 자취 목표

```
user_id, target_deposit, desired_region, house_type,
move_in_date, use_loan, created_at
```

### 3-3. 체크리스트 진행 상태

```
user_id, step_key, status, updated_at
```

---

## 4. 프론트가 받고 싶은 응답 형태

홈화면은 **API 1개**로 끝나면 제일 좋음. `GET /api/home`

```json
{
  "user": { "name": "도현", "profileImageUrl": null, "unreadCount": 3 },
  "mydata": { "lastSyncedAt": "2025-07-20" },
  "analysis": {
    "lastViewedAt": "2025-07-23",
    "nextScheduledAt": "2025-08-24"
  },
  "goal": { "targetDeposit": 30000000, "useLoan": true },
  "fund": {
    "progressRate": 42,
    "savedAmount": 12400000,
    "remainingAmount": 17600000,
    "loanAvailableAmount": 20000000,
    "monthlySavingGoal": 500000,
    "expectedMonthlySaving": 520000
  },
  "plan": {
    "current":  { "months": 14, "targetDate": "2026-09", "trend": [10,28,44,62,80] },
    "withLoan": { "months": 8,  "targetDate": "2026-03", "trend": [14,34,52,70,92] },
    "previous": { "months": 16, "analyzedAt": "2025-06-24" }
  },
  "insight": {
    "headline": "식비를 월 80,000원 절약하면\n자취 가능 시점이 2개월 앞당겨져요!",
    "tips": [
      { "label": "배달/외식 줄이기", "amount": -40000 },
      { "label": "카페 지출 줄이기", "amount": -25000 },
      { "label": "구독 서비스 점검", "amount": -15000 }
    ]
  },
  "checklist": [
    { "key": "mydata",   "label": "마이데이터 연결 확인",  "status": "완료" },
    { "key": "simulate", "label": "자취 시뮬레이션 실행",  "status": "완료" },
    { "key": "funding",  "label": "자금조달 설계 확인",    "status": "진행중" },
    { "key": "product",  "label": "추천 금융상품 비교",    "status": "대기" },
    { "key": "policy",   "label": "정책·지원금 신청 확인", "status": "대기" }
  ],
  "coach": {
    "message": "현재 소비 패턴을 유지하면 14개월 후 자취가 가능해요.\n식비 지출을 조금만 줄이면 12개월로 단축할 수 있어요!",
    "successRate": 87,
    "dsrRate": 21,
    "region": { "name": "성북구", "savingRange": "월 8~15만원" }
  }
}
```

금액은 **숫자(원 단위)** 로 주면 됨. 콤마 찍는 건 프론트에서 처리.
날짜도 `2026-09` 형태로 주면 프론트가 `2026년 09월`로 변환.

---

## 5. 아직 안 정해진 것 (팀 회의 안건)

1. **`목표 달성 확률 87%` 산출 근거** — 규칙을 정하거나, 애매하면 다른 지표로 교체
2. **`월 저축 목표`를 사용자가 직접 수정할 수 있게 할지** — 가능하다면 별도 저장 필요
3. **`대출 가능 금액 20,000,000원`의 산출 기준** — DSR 40% 기준인지, KB 상품 한도 기준인지
4. **`추천 지역`을 어디까지 계산할지** — 공공데이터 연동 vs 샘플 3~4개 지역 고정
5. **알림 기능을 실제로 만들지** — 안 만들면 헤더 뱃지는 장식용
6. **분석 주기 30일이 고정인지** — 마이데이터 갱신될 때마다 재분석할지

---

## 6. 프론트 쪽 현재 상태

- 위 값들은 전부 `src/Home.jsx` 안에 시안 숫자로 직접 적혀 있음
- API 붙일 때는 `Home.jsx` 최상단에서 `fetch('/api/home')` → 각 컴포넌트에 props로 내려주면 됨
- `previousPlan`이 없는 경우(최초 분석) 비교 문구가 숨겨지는 처리는 이미 되어 있음
