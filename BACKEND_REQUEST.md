# 백엔드 요청사항 v3 — auth / user / mydata / forecast 명세서 반영

작성 : 프론트엔드 담당
대상 : `POST /api/auth/*`, `PATCH /api/users/me/income`, `POST /api/mydata/sync`, `POST /api/forecast/simulate`

받은 명세서대로 프론트 연동을 **전부 붙였습니다.** 화면은 아래 순서로 연결돼 있습니다.

```
회원가입 → 마이데이터 연결 + 소득 입력 → 자취 목표 설정(분석) → 홈
signup     income / sync              simulate
```

이제 **서버만 켜면 바로 테스트 가능한 상태**인데, 두 가지가 막고 있습니다. 1번과 2번을 먼저 봐주세요.

---

## 1. 최우선 — CORS + Expose-Headers

### 1-1. CORS 허용 Origin

프론트는 `http://localhost:5173`(Vite), 백엔드는 `http://localhost:8080` 입니다.
포트가 다르면 브라우저가 다른 출처로 판단해 응답을 막습니다.

**Postman·curl 에서는 정상 동작합니다.** CORS 는 브라우저만 적용하는 규칙이라
서버 테스트를 다 통과해도 화면에서만 막힙니다.

### 1-2. Access-Control-Expose-Headers ← 이게 특히 중요합니다

명세서를 보면 로그인 토큰이 **응답 바디가 아니라 헤더**로 내려옵니다.

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Refresh-Token: eyJhbGciOiJIUzI1NiJ9...
```

그런데 브라우저는 CORS 요청에서 **기본적으로 6개의 안전한 헤더만** 자바스크립트에 노출합니다.
`Authorization` 과 `Refresh-Token` 은 거기에 포함되지 않습니다.

즉 **서버가 명시적으로 노출을 허용하지 않으면**

- 로그인은 `200 OK` 로 성공하고
- 응답 바디의 `userId`·`loginId`·`name` 도 잘 읽히는데
- `res.headers.get('Authorization')` 은 **항상 `null`** 이 됩니다

토큰을 못 받으니 이후 모든 인증 API 가 실패합니다.
로그인이 성공하는데 그 다음이 안 되는 형태라 원인 찾기가 아주 어려운 종류의 문제입니다.

**두 항목을 한 번에 반영한 설정** — `SecurityConfig.java`

```java
@Bean
public CorsConfigurationSource corsConfigurationSource() {
    CorsConfiguration config = new CorsConfiguration();
    config.setAllowedOrigins(List.of("http://localhost:5173"));
    config.setAllowedMethods(List.of("GET", "POST", "PATCH", "PUT", "DELETE", "OPTIONS"));
    config.setAllowedHeaders(List.of("*"));

    // ★ 이 줄이 없으면 프론트가 토큰을 읽지 못합니다
    config.setExposedHeaders(List.of("Authorization", "Refresh-Token"));

    config.setAllowCredentials(true);

    UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    source.registerCorsConfiguration("/**", config);
    return source;
}
```

기존 필터체인에 한 줄 추가:

```java
http
    .cors(cors -> cors.configurationSource(corsConfigurationSource()))   // ← 추가
    .csrf(csrf -> csrf.disable())
    ...
```

**참고** — 프론트에서는 토큰을 못 읽었을 때
`"로그인은 됐지만 토큰을 읽지 못했어요. 서버의 Access-Control-Expose-Headers 설정이 필요해요."`
라는 문구가 뜨도록 해뒀습니다. 이 메시지가 보이면 이 설정이 빠진 것입니다.

---

## 2. 확인 필요 — 인증 실패 시 HTTP 상태코드

`api.js` 는 **인증 API 응답이 `401` 이면** refresh token 으로 재발급을 시도하고,
원래 요청을 한 번 자동 재시도하도록 만들어뒀습니다.

그런데 명세서에는 `AUTH_004`(refresh token 무효) 같은 **에러 코드만** 있고
**HTTP 상태코드가 적혀 있지 않습니다.**

만약 access token 만료를 `401` 이 아닌 다른 코드(`400`·`403`·`500`)로 내려주시면
자동 갱신이 동작하지 않고 사용자가 그냥 로그아웃됩니다.

**아래처럼 맞춰주실 수 있는지 확인 부탁드립니다.**

| 상황 | 요청 상태코드 | 에러 코드 |
|---|---|---|
| access token 만료·서명 무효 | **`401`** | (신규 코드 필요) |
| refresh token 무효 | `401` | `AUTH_004` |
| 로그인 실패 | `401` 또는 `400` | `AUTH_003` |
| 아이디·이메일 중복 | `409` | `AUTH_001` / `AUTH_002` |
| 형식 검증 실패 | `400` | `COMMON_001` |
| 지원하지 않는 지역 | `400` | `SIMULATION_001` |
| 소득 미설정 | `400` | `SIMULATION_002` |

**추가 요청 하나** — access token 만료용 에러 코드를 `AUTH_004` 와 **분리**해주세요.
지금은 둘 다 `AUTH_004` 면 프론트가 "조용히 갱신하면 되는 상황"과
"로그인 화면으로 보내야 하는 상황"을 구분할 수 없습니다.
예: `AUTH_005`(access token 만료).

---

## 3. 프론트에서 처리한 내용 (확인용)

### 3-1. 검증 규칙을 서버와 똑같이 맞췄습니다

| 항목 | 적용한 규칙 |
|---|---|
| `loginId` | `^[A-Za-z]{4,20}$` — 영문만, 4~20자 |
| `password` | 10~22자 + 특수문자(영문·숫자가 아닌 문자) 1개 이상 |
| `email` | 표준 형식 |

**서버보다 엄격하게 잡지 않았습니다.** 프론트가 더 깐깐하면 서버는 통과하는 값을
화면에서 막아버려서, 나중에 규칙이 완화될 때 프론트만 고쳐야 하는 상황이 생깁니다.

`password` 의 특수문자 판정은 `[^A-Za-z0-9]` (영문·숫자가 아닌 모든 문자)로 했습니다.
**서버가 특정 기호 집합만 허용한다면 알려주세요.** 지금은 프론트가 더 관대해서
서버에서 `COMMON_001` 이 날 수 있습니다.

### 3-2. 에러 코드를 화면 문구로 매핑했습니다

10개 코드 전부 한글 문구를 붙여뒀습니다. 그리고 **어디에 표시할지 구분**했습니다.

- `AUTH_001`(아이디 중복) → 아이디 입력칸 **바로 아래**
- `AUTH_002`(이메일 중복) → 이메일 입력칸 **바로 아래**
- 그 외 → 카드 상단 **배너**

서버가 `message` 를 주면 그 문장을 우선 표시하고, 없으면 프론트 기본 문구를 씁니다.
**사용자에게 그대로 보여줄 문장으로 `message` 를 채워주시면** 프론트 수정 없이 반영됩니다.

### 3-3. 토큰 저장 위치

로그인 화면의 `로그인 상태 유지` 체크에 따라 저장소를 바꿉니다.

```
체크 O → localStorage    (브라우저를 닫아도 유지)
체크 X → sessionStorage  (탭을 닫으면 사라짐)
```

서버는 이 값을 알 필요가 없어서 요청에 담지 않습니다.

### 3-4. `estimatedMonths: null` 처리

저축 여력이 0 이하일 때 예측 불가로 내려주는 응답을 **정상 응답으로** 처리했습니다.
에러 화면이 아니라 "아직 예측할 수 없어요 + 고정 지출을 점검해보세요" 안내를 띄웁니다.

`isFallbackApplied: true` 일 때도 "평균값으로 계산했다"는 안내를 함께 보여줍니다.

---

## 4. 요청 — 회원가입 필드 5개 추가

시나리오 ① 의 회원가입 항목은 10개인데, 현재 `signup` 이 받는 건 4개입니다.
화면에서는 10개를 다 받아두고 있지만 **아래 5개는 서버로 보내지 못하고 버려집니다.**

| 필드 | 형식 | 필요한 이유 |
|---|---|---|
| `birthDate` | `"1998-01-01"` (YYYY-MM-DD) | 청년 정책 대상 연령 판정 |
| `gender` | `"남성"` / `"여성"` | 시나리오 요구 항목 |
| `job` | `"학생"` / `"취업준비생"` / `"직장인"` | 정책 조건 판정 |
| `region` | 시·도 문자열 | 지역별 정책 필터 |
| `phone` | `"010-1234-5678"` | 시나리오 요구 항목 |

**날짜는 `YYYY-MM-DD` 로 부탁드립니다.** `"19980101"` 형식은 자바스크립트 `new Date()` 가
해석하지 못해서 프론트에서 잘라 붙여야 합니다.

**주의** — `signup` 의 `region`(시·도)과 `simulate` 의 `region`(서울 25개구)은
**이름은 같지만 값의 범위가 다릅니다.** 나중에 헷갈릴 수 있어서
`residenceRegion` / `targetRegion` 처럼 구분되는 이름을 쓰는 게 안전해 보입니다.

---

## 5. 요청 — 약관 동의 저장

회원가입 화면에 동의 체크박스 3개가 있는데(필수 2 / 선택 1) 저장할 곳이 없습니다.

개인정보보호법상 **동의를 받았다는 기록**이 필요하고, 실무에서는 동의 여부만이 아니라
**동의한 시각**까지 함께 남깁니다. 심사에서 개인정보 처리를 물어볼 수 있는 부분이라
챙겨두면 좋을 것 같습니다.

```json
{
  "agreements": {
    "privacy":   { "agreed": true,  "agreedAt": "2026-07-30T14:20:11+09:00" },
    "mydata":    { "agreed": true,  "agreedAt": "2026-07-30T14:20:11+09:00" },
    "marketing": { "agreed": false, "agreedAt": null }
  }
}
```

마케팅 동의는 사용자가 나중에 철회할 수 있어야 해서, `user` 테이블 컬럼으로 두기보다
**별도 테이블에 변경 이력을 쌓는 방식**을 권합니다.

---

## 6. 요청 — 조회 API 3개

지금 만들어진 API 는 전부 **쓰기(POST/PATCH)** 입니다. 그래서 새로고침하면
화면에 아무것도 남지 않고, 홈화면은 여전히 시안 숫자가 하드코딩된 상태입니다.

### 6-1. `GET /api/mydata` — 마이데이터 조회

`POST /api/mydata/sync` 는 연동을 실행하는 API 라 화면을 열 때마다 부를 수 없습니다.
**저장된 스냅샷을 그냥 읽는 API** 가 필요합니다. 응답 형태는 `sync` 와 같으면 됩니다.

### 6-2. `GET /api/forecast` — 최근 분석 결과 조회

`POST /api/forecast/simulate` 는 결과를 사용자당 1건 upsert 한다고 되어 있으니,
그 저장된 1건을 읽는 API 가 필요합니다. 응답 형태는 `simulate` 와 같으면 됩니다.
홈화면에 들어올 때마다 재계산하면 값이 흔들리고 서버 부담도 생깁니다.

### 6-3. `GET /api/users/me` — 내 정보 조회

`name`·`loginId`·`email`·`monthlyIncome` 정도면 됩니다.
홈화면 상단 `안녕하세요, OO님` 과 소득 표시에 필요합니다.

---

## 7. 홈화면 연동을 위해 아직 없는 값

홈화면에 있는 카드 중 **현재 API 로 채울 수 없는 항목**입니다.
`GET /api/home` 하나로 묶어주시면 가장 좋고, 어려우면 위 6번 조회 API 들로 나눠도 됩니다.
필드별 계산식은 같은 레포의 **`DATA_SPEC.md`** 에 정리해두었습니다.

| 홈화면 카드 | 필요한 값 | 현재 |
|---|---|---|
| 자금 현황 | 목표 금액 대비 진행률, 모은 금액 구성(계좌/예금/적금/투자) | `sync` 로 일부 가능 |
| 자취 가능 시점 예측 | 대출 포함 시 / 미포함 시 비교, 이전 분석과의 비교 | 대출 미포함만 가능 |
| AI 추천 인사이트 | 절약 팁 3개 (항목명 + 절감액) | **없음** |
| 오늘의 체크리스트 | 5개 항목의 완료/진행중/대기 상태 | **없음** |
| AI 코치 한 마디 | 코칭 문구, 성공률, DSR 비율 | **없음** |
| 맞춤 정책·지원금 | 정책 목록 (`policy` 모듈) | 단건만 반환 |

**공통 규칙 4가지**

1. **금액은 숫자, 원 단위** — `12400000`. 콤마와 "만원" 변환은 프론트가 합니다
2. **날짜는 `2026-09` / `2025-08-24`** — `2026년 09월` 변환은 프론트가 합니다
3. **모은 금액 구성 4개의 합 = 모은 금액 총액** — 도넛과 막대가 같은 값을 나눠 쓰기 때문에 어긋나면 화면에 바로 보입니다
4. **AI 문구는 분석 시점에 만들어 DB에 저장** — 홈 조회마다 GPT를 부르면 새로고침할 때마다 문구가 바뀌고 비용도 계속 발생합니다

---

## 8. 사소한 확인 사항

- **`policy` 모듈은 이번 명세서에서 빠졌습니다.** 이전에 있던 `POST /api/policy/recommend` 가
  유지되는지, 응답을 배열로 바꾸는 요청이 반영됐는지 알려주세요. 정책 화면 설계에 필요합니다.
- **`simulate` 의 `region` 은 서울 25개구만** 지원하는 것으로 이해했습니다.
  프론트는 25개 자치구 버튼으로 만들어뒀습니다. 시·군 단위가 추가되면 알려주세요.
- **`monthlySavingCapacity` 계산식**을 알려주시면 프론트 안내 문구를 정확히 쓸 수 있습니다.
  `소득 − 총지출` 인지, 고정비만 빼는지에 따라 화면 설명이 달라집니다.
- **`assetLoan` 과 `assetRemainingRepayment`** 의 관계를 확인 부탁드립니다.
  `대출 총액` / `남은 상환액` 으로 표시했는데, mock 값이 20,000,000 / 15,000,000 이라
  이 해석이 맞는지 확인이 필요합니다.
- **`currentAsset` 이 mock 에서 `0`** 으로 나옵니다. `sync` 의 자산 합계(18,000,000)와
  연결되지 않은 것으로 보이는데, 의도된 것인지 확인 부탁드립니다.
  지금 상태로는 예측 개월 수가 실제보다 크게 나옵니다(예시 응답의 427개월).

---

## 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | 1-1 CORS | 없으면 화면에서 호출 자체가 불가 |
| 2 | 1-2 Expose-Headers | 로그인은 되는데 토큰을 못 읽음. 원인 찾기 가장 어려운 문제 |
| 3 | 8. `currentAsset` 확인 | 예측값이 크게 어긋남. 시연 시 바로 보임 |
| 4 | 2. 401 상태코드 | 자동 토큰 갱신 동작 여부가 갈림 |
| 5 | 6. 조회 API 3개 | 새로고침하면 화면이 비어버림 |
| 6 | 4. 회원가입 필드 5개 | 시나리오 ① 완성 |
| 7 | 7. 홈화면 값 | 홈이 하드코딩 상태 |
| 8 | 5. 약관 동의 저장 | 개인정보 처리 근거 |
