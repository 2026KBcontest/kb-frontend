# 백엔드 요청사항 — 개발자 1

작성 : 프론트엔드 담당
담당 범위 : **policy · product · common(에러 처리)**
확인 시점 : `main` 커밋 `66fa1e3` (PR #3 머지 직후)

먼저 반영해주신 것부터 확인했습니다. 감사합니다.
그리고 머지 결과에 문제가 하나 있어서 그것부터 적었습니다.

---

## 1. 최우선 — 머지 결과에 개발자 2 모듈이 안 들어갔습니다

"윤지 선배 코드를 기준점으로 삼고 충돌 코드를 덮어씌웠다"고 하셨는데,
**`main` 을 새로 clone 해서 확인해보니 반대로 되어 있습니다.**

```
확인 방법 : git clone --branch main https://github.com/2026KBcontest/kb-backend.git
커밋      : 66fa1e3  Merge pull request #3 from 2026KBcontest/feature/auth-signup
```

| 폴더 | `main` | `feature/forecast` |
|---|---|---|
| `auth/jwt` | **없음** | 2개 |
| `user` | **없음** | 6개 |
| `mydata` | **없음** | 6개 |
| `forecast` | **없음** | 11개 |
| `policy` | 10개 | 6개 |
| `product` | 4개 | 없음 |

컨트롤러도 `AuthController` · `PolicyController` · `ProductController` **세 개뿐**입니다.
`MyDataController` · `ForecastController` · `UserController` 가 없습니다.
`docs/` 폴더 3개(`apis.md` · `auth.md` · `forecast.md`)도 같이 사라졌습니다.

`main` 과 `feature/auth-signup` 은 **파일 기준으로 완전히 동일**합니다 (`git diff` 결과 0).

### 왜 이렇게 됐는지

`auth` 폴더 **안의** 충돌 파일만 맞추신 것 같습니다.
그런데 개발자 2 님의 작업은 `auth` 밖에도 `user` · `mydata` · `forecast` 세 폴더가 따로 있고,
**그 폴더들은 애초에 이쪽 브랜치에 없었으니 충돌로 잡히지도 않았을 것**입니다.
그래서 "충돌 없이 머지됨" 으로 보였지만 실제로는 통째로 빠진 상태가 됐습니다.

실제로 `main` 의 auth 코드를 보면 여전히 `email` 로그인이고 JWT 가 없습니다.

```java
// main 의 SignupRequest — loginId 없음
private String email;
private String password;
private String name;
```

### 다시 해주실 순서

```
1. feature/forecast 를 main 으로 먼저 머지
2. 그 위에 policy · product · common 얹기
```

이렇게 하면 개발자 2 님 모듈 4개가 살아있는 상태에서 이쪽 작업만 추가됩니다.
`policy` · `product` 는 개발자 2 님 브랜치에 없는 폴더라 충돌 없이 그대로 올라갑니다.

**두 브랜치 차이는 아래 명령으로 확인하실 수 있습니다.**

```bash
git diff main feature/forecast --stat
```

---

## 2. 반영 확인 — 잘 들어갔습니다

| 요청 항목 | 확인 내용 |
|---|---|
| **401 · 409 핸들러** | `UnauthorizedException` · `DuplicateEmailException` 추가 확인 |
| **온통청년 API 키 분리** | `${YOUTH_API_KEY:default_key_here}` 로 환경변수 처리 확인 |
| **`GET /api/products`** | `ProductController` 신규 추가 확인 |
| 정책 `policies` 배열 + `aiReason` | `PolicyResponse` 확인 |
| `description` 25자 제한 | `PolicyService` 의 substring 처리 확인 |
| `status` 뱃지 | `신청 가능` / `조건 확인 필요` 두 값만 사용 |
| CORS `localhost:5173` | `SecurityConfig` 확인 |
| 에러 메시지 노출 | `server.error.include-message=always` 확인 |
| 금액 원 단위 · 생년월일 `YYYY-MM-DD` | `PolicyRequest` 확인 |

**이 세 가지(401·409 / API 키 / ProductController)는 개발자 2 브랜치에 없는 것들이라,
다시 머지하실 때 꼭 같이 올려주세요.**

프론트도 새 에러 코드(`UNAUTHORIZED` · `DUPLICATE_EMAIL`)에 한글 문구를 붙여뒀고,
이메일 중복이면 이메일 입력칸 아래에 바로 뜨도록 연결했습니다.

**API 키 관련 한 가지만 더** — 이미 커밋된 키(`50b0b237-...`)는 git 히스토리에 남아 있습니다.
여유가 되면 온통청년에서 **재발급**받는 것도 권합니다.

---

## 3. 요청 — `GET /api/products` 응답 형태 조정

컨트롤러가 생긴 건 확인했는데, 지금 형태로는 화면을 만들기 어려운 부분이 있습니다.

**현재 응답**

```json
[
  { "id": "prod-1", "name": "KB 청년 맞춤형 전세자금대출",
    "category": "LOAN", "maxAmount": 200000000, "interestRate": "2.4%" }
]
```

**세 가지만 바꿔주시면 좋겠습니다.**

**① 배열을 객체로 감싸주세요** — `{ "products": [ ... ] }`
나중에 `totalCount` 나 `aiReason` 같은 값을 추가할 때 응답 구조를 안 바꿔도 됩니다.
`policy` 는 이미 `{ policies, aiReason }` 형태라 그쪽과도 맞습니다.

**② `link` 가 필요합니다** — 상품 카드의 `자세히 보기` 버튼을 연결할 곳이 없습니다.
KB 상품 페이지 URL 을 넣어주세요.

**③ 항목 표시가 상품마다 달라서 `specs` 배열이면 좋겠습니다**
대출은 `금리 / 한도 / 상환기간`, 적금은 `금리 / 월납입한도 / 기간` 처럼 항목이 달라집니다.
지금처럼 `maxAmount` · `interestRate` 고정 필드면 적금에 `maxAmount 700000` 이 들어가
"최대 70만원 대출" 처럼 읽힐 수 있습니다.

```json
{
  "products": [
    {
      "productId": "kb-youth-jeonse-loan",
      "name": "KB 청년 맞춤형 전세자금대출",
      "category": "LOAN",
      "tag": "최저 금리",
      "specs": [
        { "label": "최저 금리", "value": "2.40%" },
        { "label": "예상 한도", "value": "2억원" },
        { "label": "상환 기간", "value": "최대 10년" }
      ],
      "link": "https://obank.kbstar.com/..."
    }
  ]
}
```

`tag` 는 카드 우측 상단 뱃지용입니다. 없으면 `null` 로 주시면 프론트가 숨깁니다.

**④ (추가) 계산에 쓸 숫자 필드도 같이 주세요** — 화면을 만들면서 새로 필요해진 부분입니다.

자금조달 설계 화면에서 **상품을 고르면 그 조건으로 DSR 을 계산**합니다.
그런데 `specs` 는 사람이 읽는 문자열이라(`"최대 10년"`, `"2.40%"`) 계산에 쓸 수 없습니다.
`"최대 2억원"` 에서 숫자만 뽑아내는 건 표기가 바뀌면 바로 깨집니다.

```json
{
  "productId": "kb-youth-jeonse-loan",
  "name": "KB 청년 맞춤형 전세자금대출",
  "specs": [ ... ],              // 화면 표시용 (그대로 유지)

  "calc": {                      // 계산용 — 숫자만
    "maxLimit": 200000000,       // 한도 (원)
    "minRate": 2.4,              // 최저 금리 (%)
    "maxRate": 4.1,              // 최고 금리 (%)
    "maxYears": 10               // 최장 상환 기간 (년)
  }
}
```

이 네 개만 있으면 프론트가 **월 상환액과 DSR 을 원리금균등 공식으로 계산**해서
"이 상품으로 빌리면 DSR 32%, 월 58만원" 처럼 보여줄 수 있습니다.
`KbProduct` 엔티티에 `minInterestRate` · `maxInterestRate` · `maxLimit` 이 이미 있으니
`maxYears` 만 추가하면 될 것 같습니다.

**한 가지 확인** — `kb_products.json` 과 `MockKbCoreBankingClient` 를 만들어두셨는데
컨트롤러에는 상품이 하드코딩되어 있습니다. 그 파일들을 쓰실 계획이면
**`kb_products.json` 의 필드 구조를 알려주세요.** 그 구조에 맞춰 화면을 만들겠습니다.

---

## 4. 요청 — 에러 코드 체계를 하나로

지금 **세 벌**이 섞여 있습니다.

```
AUTH_001 ~ COMMON_001            개발자 2 (forecast 브랜치)
UNAUTHORIZED / DUPLICATE_EMAIL   개발자 1 (main 신규)
INVALID_INPUT / BAD_REQUEST      개발자 1 (기존)
```

응답 형식도 다릅니다.

```
개발자 1 →  { "code": "UNAUTHORIZED", "message": "..." }
개발자 2 →  { "success": false, "errorCode": "AUTH_003", "message": "...", "timestamp": "..." }
```

**프론트는 셋 다 읽도록 처리해뒀습니다** (`errorCode ?? code`). 당장 깨지지는 않습니다.
다만 화면에서 "이 에러면 이렇게 안내" 같은 분기를 걸 때 코드 체계가 여러 벌이면
어느 쪽이 올지 몰라 조건을 두 번씩 써야 합니다.

개발자 2 님 체계(`errorCode` + `AUTH_001`~`COMMON_001`)가 상황별로 세분화돼 있어
프론트에서 쓰기 좋습니다. **두 분이 상의해서 하나로 정해주세요.**

정해지면 프론트에서 관용 처리를 지우겠습니다.

---

## 5. 확인 요청 3개

**① 온통청년 API 인증키 발급 상태**
`${YOUTH_API_KEY:default_key_here}` 로 기본값이 `default_key_here` 라, 환경변수를 안 넣으면
실제 호출은 실패하고 fallback 2건만 나옵니다.
시연 때 정책이 2건만 보이면 화면이 비어 보이니, **발급 예정이 없다면 fallback 목록을
3~4건으로 늘려주시면** 좋겠습니다.

**② `PolicyRequest.region` 의 값 범위**
주석에 `"서울, 경기"` 라고 되어 있어 **시·도 단위**로 이해했습니다.
프론트는 `"서울특별시"` 처럼 정식 명칭으로 보내고 있는데, `"서울"` 형태를 기대하신다면
알려주세요.

**주의** — `forecast` 의 `region` 은 **서울 25개 자치구**(`"강남구"`)입니다.
이름은 같은데 값의 범위가 달라서, 회원가입 필드는 `residenceRegion` 으로
이름을 나누자고 개발자 2 님께 제안해뒀습니다.

**③ `/api/policy/**` 인증 필요 여부**
개발자 2 님 `SecurityConfig` 에서는 `permitAll` 입니다.
로그인 없이도 정책 추천이 되는 게 맞나요? 맞다면 홍보 페이지에도 정책 미리보기를
넣을 수 있어 좋습니다.

---

## 6. 요청 — `policy/recommend` 가 회원 정보를 직접 읽는 방식 검토

정책 화면(시나리오 ⑦)을 만들면서 나온 이야기입니다. 결론부터 적으면
**두 가지 방식 중 하나를 골라주시면 되고, 프론트는 어느 쪽이든 맞추겠습니다.**

### 지금 구조의 문제

```
POST /api/policy/recommend   { region, income, birthDate }
```

이 세 값은 **회원가입에서 이미 입력받은 값**입니다. 그런데 `SignupRequest` 가 4개 필드만
받아서 서버에 저장되지 않고, 결국 정책 화면에서 **사용자에게 다시 물어봐야 합니다.**

```
회원가입 : 생년월일·거주지역 입력  →  서버로 안 감 (버려짐)
정책 화면 : 생년월일·지역 다시 입력  →  "방금 적었는데 왜 또?"
```

프론트는 임시로 브라우저에 담아뒀다가 자동으로 채우고, 값이 있으면 화면에 들어오자마자
자동 조회하도록 해뒀습니다. 다만 탭을 닫거나 다른 기기에서 로그인하면 사라집니다.

### 안 A — 지금 형태 유지 (요청 본문 그대로)

개발자 2 님께 `user` 테이블에 `birth_date` · `residence_region` 컬럼 추가와
`GET /api/users/me` 응답 확장을 요청해뒀습니다 (`BACKEND_REQUEST_2D.md` 12번).

이게 반영되면 프론트가 회원 정보를 한 번 불러서 **요청 본문을 알아서 채웁니다.**
**`policy` 쪽은 코드를 안 고치셔도 됩니다.**

- 장점 : policy 모듈 수정 없음. 로그인 없이도 정책 조회 가능(홍보 페이지 미리보기 가능)
- 단점 : 프론트가 조건을 만들어 보내므로, 값이 없으면 여전히 물어봐야 함

### 안 B — 토큰으로 회원을 식별해서 서버가 조건을 채움

```java
@PostMapping("/recommend")
public PolicyResponse recommend(@AuthenticationPrincipal UserDetails user) {
    User me = userRepository.findByLoginId(user.getUsername());
    // birthDate · residenceRegion · monthlyIncome 을 여기서 읽어서 필터링
}
```

- 장점 : 프론트가 조건을 보낼 필요가 없고, 입력칸을 아예 없앨 수 있음.
  기기가 바뀌어도 항상 동일한 결과
- 단점 : `SecurityConfig` 의 `/api/policy/**` 를 `permitAll` → `authenticated` 로 바꿔야 함.
  **로그인 전 정책 미리보기는 불가능해집니다.**
  개발자 2 님의 `user` 모듈에 의존하게 되어 모듈 간 결합도 생깁니다

### 제 의견

**안 A 를 권합니다.** 이유 두 가지입니다.

1. `policy` 모듈이 `user` 모듈에 의존하지 않아 두 분의 작업이 계속 분리됩니다
2. 로그인 없이도 정책을 볼 수 있어서, 홍보 페이지에 "이런 정책을 찾아드려요" 미리보기를
   넣을 수 있습니다 (심사에서 보여줄 거리가 하나 늘어납니다)

다만 이건 제 판단이고, **팀에서 안 B 로 가기로 하면 프론트를 그쪽으로 고치겠습니다.**
어느 쪽이든 `BACKEND_REQUEST_2D.md` 12번(회원 프로필 저장)은 필요합니다.

### 안 A 로 갈 경우 확인 부탁드리는 것 2개

**① `income` 은 연소득인가요, 월소득인가요?**
`PolicyRequest` 주석에 `연소득 5천만원 이하` 같은 조건이 있는데 필드명은 `income` 입니다.
프론트는 지금 **월 소득(원 단위)** 을 보내고 있습니다. 연소득이면 프론트에서 ×12 해서
보내겠습니다. 필드명을 `monthlyIncome` / `annualIncome` 로 명확히 해주시면 더 좋습니다.

**② 조건에 맞는 정책이 0건일 때 응답 형태**
`{ "policies": [], "aiReason": null }` 로 내려주시면 프론트가 "조건에 맞는 정책을 찾지 못했어요"
화면을 띄웁니다. 404 나 에러로 내려주시면 실패 문구가 떠서 사용자가 오해합니다.

---

## 7. 제안 — AI 추천을 실제로 붙이는 방법 (중요)

공모전 주제가 **AI Challenge** 인데, 지금 코드에서 AI 라고 부를 수 있는 부분이 없습니다.
지적이 아니라, 심사에서 반드시 나올 질문이라 미리 정리해 드립니다.

### 지금 상태

```java
// PolicyService.getRecommendedPolicy()
int limit = Math.min(dbPolicies.size(), 4);
for (int i = 0; i < limit; i++) { ... }   // DB 앞에서 4개를 그냥 자름

String aiReason = "현재 연령·소득 조건 및 자취 목표를 종합 고려할 때 …";  // 고정 문자열
```

`PolicyRequest` 로 `region` · `income` · `birthDate` 를 **받아놓고 쓰지 않습니다.**
어떤 조건을 넣어도 같은 정책이 같은 순서로 나옵니다.
`aiReason` 도 상수라 사용자가 달라져도 문장이 같습니다.

**"AI가 어디에 쓰였나요?" 라는 질문에 답하기 어려운 상태입니다.**

### 여기가 AI 가 정말 필요한 자리입니다

온통청년 API 가 주는 값은 **비정형 한글 문장**입니다.

```
ageInfo : "만 19세~34세 이하 무주택 세대주로서 연소득 5천만원 이하인 자"
summary : "청년층의 주거비 부담 완화를 위해 전월세 보증금을 …"
```

이런 문장에서 나이·소득·주택 보유 조건을 뽑아 사용자와 대조하는 건
**정규식이나 if 문으로 짜기 매우 어렵습니다.** 정책마다 표현이 다르기 때문입니다.
반대로 **LLM 이 가장 잘하는 일**이기도 합니다.

즉 "AI 를 썼다" 가 아니라 **"AI 가 아니면 못 한다"** 고 말할 수 있는 지점입니다.

### 요청 — GPT 호출은 서버에서

```
POST /api/policy/recommend   (기존 API 를 확장)

응답에 aiReason 대신 구조화된 결과를 넣어주세요
{
  "policies": [ ... ],
  "recommendation": {
    "source": "ai",
    "pick": "청년 월세 지원",
    "headline": "청년 월세 지원을 먼저 신청하세요",
    "reasons": [
      { "label": "나이", "value": "만 27세", "note": "지원 대상 만 19~39세에 해당" },
      { "label": "소득", "value": "월 240만원", "note": "중위소득 150% 이하 충족" }
    ],
    "alternative": "버팀목 전세자금대출은 보증금이 모인 뒤 검토하는 편이 좋아요"
  }
}
```

**프론트는 이 형태를 받을 준비가 끝나 있습니다.** `AiPick.jsx` 가 그대로 그립니다.
지금은 `source: "rule"` 로 프론트가 조건 판단한 결과를 보여주고 있고,
화면에도 **"조건 기반 추천"** 이라고 정직하게 표기했습니다.
서버가 `source: "ai"` 를 주는 순간 배지가 **"AI 추천"** 으로 바뀝니다. 화면 수정은 없습니다.

### 프롬프트 설계 — 숫자는 GPT 에게 맡기지 마세요

**가장 중요한 부분입니다.** 생성형 모델은 그럴듯한 숫자를 지어냅니다.
"최대 2억 4천만원까지 대출 가능합니다" 같은 문장이 근거 없이 나오면
금융 서비스로서 치명적이고, 심사에서 바로 지적받습니다.

```
[GPT 에게 주는 것]
  - 정책 목록 (제목·조건문·신청 링크)
  - 사용자 조건 (나이·소득·지역)   ← 서버가 계산한 값
  - 자취 목표 (필요 자금·부족액)   ← 서버가 계산한 값

[GPT 에게 시키는 것]
  ① 어느 정책을 먼저 신청할지 고르기
  ② 왜 그것인지 (위에서 준 값만 인용해서)
  ③ 다른 정책은 왜 지금이 아닌지

[GPT 가 하면 안 되는 것]
  - 금액·기간·한도를 새로 만들어내기
  - 목록에 없는 정책을 언급하기
```

시스템 프롬프트에 **"제공된 값만 인용하고 새로운 숫자를 만들지 마시오"** 를 넣고,
응답을 JSON 으로 강제(structured output)하면 안정적입니다.

### 실무적인 부분 두 가지

**① 호출 횟수** — 정책 화면에 들어올 때마다 GPT 를 부르면 비용도 늘고 느립니다.
사용자 조건이 그대로면 결과도 같으니, `(userId + 조건)` 으로 캐시하거나
`youth_policy` 테이블에 조건 추출 결과를 미리 저장해두는 방법이 있습니다.

**② 실패 대비** — GPT 가 실패하거나 느릴 때 화면이 비면 안 됩니다.
`recommendation` 을 못 만들면 그냥 빼고 `policies` 만 주세요.
프론트가 알아서 조건 기반 추천으로 대체합니다.

---

## 7-2. 요청 — 정책 응답에 '지원 금액' 을 넣어주세요

자금조달 설계 화면은 필요 자금을 **자기자본 + 지원금 + 대출** 로 나눠 보여줍니다.
그런데 정책 응답에 금액이 없어서 **지원금 칸이 계속 0원**입니다.

```
필요 초기자금  11,552,000원
 ├ 자기자본     3,000,000원   마이데이터 순자산
 ├ 지원금               0원   ← 금액 데이터가 없어서 비어 있음
 └ 대출                 0원   ← 상품 API 가 없어서 비어 있음
```

정책 항목에 아래 두 개만 추가해주시면 지원금 칸이 채워집니다.

```json
{
  "policyId": "monthly-rent",
  "policyName": "서울시 청년 월세 지원",
  "supportAmount": 2400000,       // 총 지원 금액 (원). 모르면 null
  "supportNote": "월 20만원 × 12개월"   // 계산 근거 한 줄
}
```

**모르면 `null` 로 주세요.** 지어낸 금액이 들어가면 자금 계획 전체가 틀어집니다.
`null` 이면 프론트는 지금처럼 0원으로 두고 '확인 전' 이라고 표시합니다.

온통청년 API 가 금액을 정형화된 필드로 주지 않는다면, **이것도 7번의 AI 가 할 일**입니다.
`summary` 문장("월 최대 20만원을 12개월간 지원")에서 금액을 뽑아내는 건
LLM 이 잘하는 작업이고, 정책마다 표현이 달라 규칙으로는 어렵습니다.

---

## 7-3. 요청 — AI 어드바이스 API (`POST /api/ai/advice`)

세 화면(맞춤 정책 · 저축 플랜 · 자금조달)에 **AI 어드바이스** 카드를 하나씩 뒀습니다.
7번의 추천과 이 질문·답변이 **같은 카드 안에서** 표시됩니다.

```
AI 어드바이스                              후보 2건 중  [조건 기반]

  💡 청년 버팀목 전세자금대출을 먼저 확인해보세요      ← 7번 (추천)
     · 나이  만 27세 — 만 19~34세 / 연소득 5천만원 이하
     · 월 소득  2,400,000원
     · 신청 상태  신청 가능

  AI에게 물어보기                                     ← 7-3 (질문·답변)
  [이 중에서 뭐부터 신청해야 하나요?] [제가 못 받는 정책이 있나요?]
```

**질문을 누르면 그 자리에 답변이 들어옵니다.** 배지도 `조건 기반` → `AI 분석` 으로 바뀝니다.

**프론트는 이미 호출하고 있습니다.** 서버에 API 가 없어서 안내만 뜨는 상태입니다.

```
POST /api/ai/advice
Authorization: Bearer {accessToken}

{
  "scope": "funding",                    // policy | saving | funding
  "question": "대출을 받는 게 나을까요, 더 모으는 게 나을까요?",
  "context": {                           // 그 화면이 계산해둔 값
    "required": 11552000,
    "own": 3000000,
    "support": 0,
    "loan": 0,
    "dsr": 4.2,
    "monthlyPayment": 100000
  }
}

응답
{
  "source": "ai",
  "text": "지금은 대출보다 저축을 이어가는 편이 나아요. …",
  "updatedAt": "2026-07-31T15:20:00+09:00"
}
```

**`context` 를 함께 보내는 이유** — 7번에 적은 것과 같습니다.
AI 가 인용할 숫자를 먼저 줘야 새로운 숫자를 지어내지 않습니다.
시스템 프롬프트에 **"context 의 값만 인용하고 새 숫자를 만들지 마시오"** 를 넣어주세요.

**답변 길이는 3~4문장으로 제한**해주시면 좋겠습니다. 카드 안에 들어가는 분량이고,
길면 아무도 안 읽습니다.

7번(추천)과 이 API 는 **프롬프트만 다르고 구조는 같습니다.** 한 번에 만드시면 됩니다.

**둘 중 하나만 먼저 해도 화면은 동작합니다.**
`policy/recommend` 의 `recommendation` 만 오면 카드 윗부분이 AI 로 바뀌고,
`ai/advice` 만 오면 질문 답변이 동작합니다. 순서는 편하신 대로 하셔도 됩니다.

---

## 8. 참고 — 개발자 2 께 요청한 것

중복 요청을 피하려고 적어둡니다. 상세 내용은 `BACKEND_REQUEST_2D.md` 에 있습니다.

- CORS + `setExposedHeaders` 추가 (forecast 브랜치에 CORS 가 없음)
- 필요 환경변수 문서화 (`JWT_SECRET` 은 32자 이상이어야 함)
- `loginId` 에 숫자 허용
- 인증 실패 시 `401` 로 통일 + 만료 에러코드 분리
- 조회 API 3개 (`GET /api/mydata` · `/api/forecast` · `/api/users/me`)
- 회원가입 필드 5개 추가 · 약관 동의 저장

**`birthDate`·`job` 을 앞으로 쓰신다고 하신 부분** — 회원가입 API 가 개발자 2 브랜치에 있어서
그쪽에 필드 추가를 요청해뒀습니다. 지금은 화면에서 입력은 받지만 서버로 못 보내고
버려지는 상태입니다.

---

## 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | 1. 재머지 (forecast 먼저) | 지금 main 으로는 회원가입 이후가 전부 동작 안 함 |
| 2 | **7. AI 추천 (GPT 연동)** | **공모전 주제가 AI인데 지금 AI 라고 부를 부분이 없음** |
| 2 | 6. 정책 조건 전달 방식 (안 A / 안 B) | 정하기 전까지 정책 화면에서 조건을 다시 물어봐야 함 |
| 3 | 6-① `income` 이 연소득인지 월소득인지 | **지금 틀린 값을 보내고 있을 수 있음** |
| 4 | 3. `products` 응답 형태 (④ `calc` 포함) | 상품을 골라도 DSR 을 계산할 수 없음 |
| 5 | 7-2. 정책 `supportAmount` | 자금조달 도넛의 '지원금' 이 계속 0원 |
| 6 | 7-3. AI 어드바이스 API | 세 화면에 자리를 만들어뒀는데 비어 있음 (7번과 같이 만들면 됨) |
| 7 | 4. 에러 코드 통일 | 프론트가 임시로 세 벌 다 처리 중 |
| 8 | 5-① 온통청년 키 / fallback | 시연 때 정책이 2건만 나옴 |
| 9 | 6-② 0건일 때 응답 형태 | 404 면 실패 문구가 떠서 오해함 |
| 10 | 5-②③ 확인 사항 | 값 범위 · 인증 여부 |
| 11 | 2. API 키 재발급 | 히스토리에 남아 있음 |

**7 · 7-2 · 7-3 은 사실상 한 덩어리입니다.** GPT 를 한 번 붙이면 세 개가 같이 해결됩니다.
프론트는 세 곳 모두 받을 준비가 끝나 있어서, 서버 응답만 오면 화면 수정 없이 켜집니다.
